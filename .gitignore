node_modules/
out/
dist/
release/
*.log
.DS_Store
.vite/
coverage/
test-results/
playwright-report/

# Incremental type-check state. It is a cache keyed by absolute paths on the
# machine that wrote it, so a tracked copy is a file that changes on every
# build and is useful to nobody else.
*.tsbuildinfo

# Which certificate this machine signs with: a keychain identity and a
# notarytool profile, both local facts. See scripts/signing.sh.
.signing.local

# A preview of the icon at the size it is judged at, drawn by `make icon`.
build/icon-64.png

# Copied in from resources/icon.svg by make; the drawing itself is tracked there.
src/renderer/assets/icon.svg
